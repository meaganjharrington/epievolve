% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/final_estimate_Rt.R
\name{final_estimate_Rt}
\alias{final_estimate_Rt}
\title{Estimate Rt(t) from daily new cases (deterministic SIR, Poisson) — NO PRIOR
Estimating BOTH beta and gamma, with user-specified fixed I0}
\usage{
final_estimate_Rt(
  incidence,
  N,
  I0,
  n_steps = 4000,
  burnin = 0.5,
  proposal_vcv = diag(c(0.02, 0.02)^2),
  init_beta = 0.3,
  init_gamma = 1/7,
  seed = 4,
  n_rt_draws = 300
)
}
\arguments{
\item{incidence}{data.frame with columns:
\itemize{
\item \code{time}: consecutive daily integers (e.g., 1,2,...,T)
\item \code{cases}: non-negative counts (NEW infections per day)
}}

\item{N}{Population size (positive scalar)}

\item{I0}{Initial infected (positive scalar, REQUIRED)}

\item{n_steps}{MCMC iterations (default 4000)}

\item{burnin}{Fraction of iterations to discard (default 0.5)}

\item{proposal_vcv}{2x2 proposal variance-covariance on (log_beta, log_gamma).
Default diag(c(0.05, 0.05)^2).}

\item{init_beta}{Initial guess for beta (default 0.3)}

\item{init_gamma}{Initial guess for gamma (default 1/7)}

\item{seed}{RNG seed (default 4)}

\item{n_rt_draws}{Number of posterior draws to use for Rt(t) intervals
(default 300; set 0 for median-only)}
}
\value{
list:
\itemize{
\item \code{samples}: data.frame(beta, gamma, R0)
\item \code{estimates}: medians & 95\% CIs for beta, gamma, R0
\item \code{Rt_series}: data.frame(time, Rt_median, Rt_lower, Rt_upper)
\item \code{model_used}: "SIR_deterministic_beta_gamma"
}
}
\description{
\itemize{
\item Deterministic odin2/dust2 trajectory
\item Observed NEW incidence via Poisson likelihood on inc_step
\item Two parameters (beta, gamma) inferred with likelihood-only MCMC on log-scale
\item I0 is FIXED and must be supplied by the user
\item Rt(t) = beta * S(t) / (gamma * N), reported at posterior-median beta,gamma
}
}
